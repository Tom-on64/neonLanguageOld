const errors = {
  0: "Unknown Error",
  200: "Expected $0, but got $1!", 
  201: "Unexpected keyword $0!", 
  300: "Class $0 tried to inherit from $1, but $1 does not exist!"
};

export default errors;